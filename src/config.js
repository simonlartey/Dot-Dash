// Tunable constants — all feel-related numbers live here, not scattered in code.
// Milestone 1 (MVP) keeps this small; the difficulty ramp lands in Milestone 2.
export const CONFIG = {
  roundSeconds: 30, // length of a round
};
